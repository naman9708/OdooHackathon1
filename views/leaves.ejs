<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leave Management - Dayflow</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            min-height: 100vh;
            color: #fff;
        }
        .header {
            background: #0f0f1e;
            border-bottom: 2px solid #444;
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .nav {
            display: flex;
            gap: 20px;
            align-items: center;
        }
        .nav-item {
            padding: 8px 20px;
            color: #fff;
            text-decoration: none;
            transition: background 0.3s;
            border-radius: 5px;
        }
        .nav-item:hover {
            background: #2a2a3e;
        }
        .company-name {
            font-size: 24px;
            font-weight: bold;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .container {
            padding: 30px;
            max-width: 1200px;
            margin: 0 auto;
        }
        .btn {
            background: #8b5cf6;
            color: #fff;
            border: none;
            padding: 12px 30px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            border-radius: 5px;
            margin-bottom: 30px;
            transition: all 0.3s;
        }
        .btn:hover {
            background: #7c3aed;
            transform: translateY(-2px);
        }
        .btn-small {
            padding: 8px 15px;
            font-size: 14px;
            margin: 0 5px;
        }
        .btn-success {
            background: #10b981;
        }
        .btn-danger {
            background: #ef4444;
        }
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }
        .modal.active {
            display: flex;
        }
        .modal-content {
            background: #1e1e2e;
            border: 2px solid #444;
            border-radius: 10px;
            padding: 30px;
            max-width: 500px;
            width: 90%;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 8px;
            color: #888;
            font-weight: 500;
        }
        input, select, textarea {
            width: 100%;
            padding: 12px;
            background: #0f0f1e;
            border: 1px solid #444;
            border-radius: 5px;
            color: #fff;
            font-size: 14px;
            font-family: 'Segoe UI', sans-serif;
        }
        input:focus, select:focus, textarea:focus {
            outline: none;
            border-color: #667eea;
        }
        .table-container {
            background: #1e1e2e;
            border: 2px solid #444;
            border-radius: 10px;
            padding: 25px;
            overflow-x: auto;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #444;
        }
        th {
            color: #667eea;
            font-weight: 600;
        }
        tr:last-child td {
            border-bottom: none;
        }
        tr:hover {
            background: #2a2a3e;
        }
        .status-badge {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 15px;
            font-size: 12px;
            font-weight: 600;
        }
        .status-pending {
            background: #f59e0b;
            color: white;
        }
        .status-approved {
            background: #10b981;
            color: white;
        }
        .status-rejected {
            background: #ef4444;
            color: white;
        }
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #888;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="nav">
            <div class="company-name">Dayflow</div>
            <a href="/dashboard" class="nav-item">Dashboard</a>
            <% if (isAdmin) { %>
                <a href="/employees" class="nav-item">Employees</a>
            <% } else { %>
                <a href="/profile" class="nav-item">Profile</a>
            <% } %>
            <a href="/attendance" class="nav-item">Attendance</a>
            <a href="/leaves" class="nav-item">Time Off</a>
        </div>
        <div>
            <a href="/logout" class="nav-item">Logout</a>
        </div>
    </div>

    <div class="container">
        <h1 style="margin-bottom: 30px;">Leave Management</h1>

        <% if (!isAdmin) { %>
            <button class="btn" onclick="openModal()">Apply for Leave</button>
        <% } %>

        <div class="table-container">
            <% if (records.length === 0) { %>
                <div class="empty-state">
                    <h3>No leave requests found</h3>
                    <p><% if (!isAdmin) { %>Click "Apply for Leave" to submit a request<% } %></p>
                </div>
            <% } else { %>
                <table>
                    <thead>
                        <tr>
                            <% if (isAdmin) { %>
                                <th>Employee</th>
                            <% } %>
                            <th>Type</th>
                            <th>Start Date</th>
                            <th>End Date</th>
                            <th>Remarks</th>
                            <th>Status</th>
                            <% if (isAdmin) { %>
                                <th>Actions</th>
                            <% } %>
                        </tr>
                    </thead>
                    <tbody>
                        <% records.sort((a, b) => new Date(b.appliedDate) - new Date(a.appliedDate)).forEach(leave => { %>
                            <tr>
                                <% if (isAdmin) { %>
                                    <td><%= leave.employeeName %></td>
                                <% } %>
                                <td><%= leave.leaveType %></td>
                                <td><%= new Date(leave.startDate).toLocaleDateString() %></td>
                                <td><%= new Date(leave.endDate).toLocaleDateString() %></td>
                                <td><%= leave.remarks || 'N/A' %></td>
                                <td>
                                    <span class="status-badge status-<%= leave.status %>">
                                        <%= leave.status.charAt(0).toUpperCase() + leave.status.slice(1) %>
                                    </span>
                                </td>
                                <% if (isAdmin && leave.status === 'pending') { %>
                                    <td>
                                        <form method="POST" action="/leaves/approve/<%= leave.id %>" style="display: inline;">
                                            <button type="submit" class="btn btn-small btn-success">Approve</button>
                                        </form>
                                        <form method="POST" action="/leaves/reject/<%= leave.id %>" style="display: inline;">
                                            <button type="submit" class="btn btn-small btn-danger">Reject</button>
                                        </form>
                                    </td>
                                <% } else if (isAdmin) { %>
                                    <td>-</td>
                                <% } %>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } %>
        </div>
    </div>

    <% if (!isAdmin) { %>
    <div id="leaveModal" class="modal">
        <div class="modal-content">
            <h2 style="margin-bottom: 20px;">Apply for Leave</h2>
            <form method="POST" action="/leaves/apply">
                <div class="form-group">
                    <label>Leave Type</label>
                    <select name="leaveType" required>
                        <option value="Paid">Paid Leave</option>
                        <option value="Sick">Sick Leave</option>
                        <option value="Unpaid">Unpaid Leave</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Start Date</label>
                    <input type="date" name="startDate" required>
                </div>
                <div class="form-group">
                    <label>End Date</label>
                    <input type="date" name="endDate" required>
                </div>
                <div class="form-group">
                    <label>Remarks</label>
                    <textarea name="remarks" rows="3"></textarea>
                </div>
                <div style="display: flex; gap: 10px;">
                    <button type="submit" class="btn">Submit</button>
                    <button type="button" class="btn btn-danger" onclick="closeModal()">Cancel</button>
                </div>
            </form>
        </div>
    </div>
    <% } %>

    <script>
        function openModal() {
            document.getElementById('leaveModal').classList.add('active');
        }
        function closeModal() {
            document.getElementById('leaveModal').classList.remove('active');
        }
    </script>
</body>
</html>